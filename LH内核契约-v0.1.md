# Learning Harness（LH）内核契约 v0.1

> 本文档是 LH 项目的架构宪法，供开发者与 coding agent 共读。
> 内容定位：内核语法 + 不变量 + 验证方法。一切需要"判断"的事物不在本文档管辖范围内——它们属于插件。

\---

## 0\. 项目定位

LH 是一个面向学习的 AI Harness，解决 AI 辅助学习的两个核心痛点：

1. **上下文管理**：一次学习短则数周、长则数月乃至更久。事件系统与会话持久化本质上是上下文管理问题——学习产品的上下文观是"一段跨越数月的、有结构的成长史"，而非"一段对话"。
2. **因材施教**：教学规划与学习辅助必须建立在大模型对用户的界定之上。"材"有双面：**作为学生的用户**（学习者模型）与**用户持有的学习资源**（资源模型）。匹配是双向的。

明确拒绝的两条路线：

* **百万上下文力大砖飞**：成本拒绝广大学习者；且 lost-in-the-middle、注意力稀释意味着质量本身随上下文规模退化。
* **硬编码 skills 覆盖教学法**：把教学法知识冻进代码，无法生长、无法被懂教学的人编辑。

\---

## 1\. 总原则

### 1.1 内核拥有语法，插件拥有判断

* **内核**：schema、信封、注册表、投影机制、context compiler、策略解释器等"机制"。冻结优先级最高。
* **插件**：分类、建模、策略、记录——一切会错、会进化、会因人而异的事物。

判断一个部件归属的试金石：**"它做的是格式约定还是内容裁决？"** 格式归内核，裁决归插件。

### 1.2 事件溯源为基座

* append-only 事件日志是唯一事实源；一切状态是日志的投影。
* 两个核心痛点共享同一基座：上下文管理是日志的一种读法（投影成 context），因材施教是另一种读法（投影成学习者模型），教学动作是写回。

### 1.3 写细读粗

* 写入端往细记：存储便宜，"没记"不可逆。
* 读取端由投影定义粗粒度：语义单位（如"一次解题 episode"）是原子事件上的投影。
* 写入粒度与读取粒度解耦，不做终极抉择。

### 1.4 内核连"什么值得记"都不知道

* 记录器是插件（对话记录器、操作记录器、资源记录器……）。内核不裁决操作元数据的学习语义。
* 后果一：重放不变量是相对的——"给定同一插件集合，重放结果确定"。出处字段（插件 id + 版本）是重放正确性的前提。
* 后果二：晚装插件看不到无人记录的数据。解法在发行策略：**内核保持无知，发行版有立场**——官方发行版默认捆绑一套记录器插件。
* 后果三：跨插件互操作靠生态约定而非内核强制（类比 OpenTelemetry：内核是协议，词汇表是约定，约定活在官方插件与文档里）。

\---

## 2\. 内核五概念

内核的全部概念：**节点、事件、投影、handoff、agent 注册表**。其余皆是插件。

### 2.1 节点（课程树）

> 课程节点 = 一个作用域 + 自己的事件日志 + 自己的 agent + 自己的建模插件 + 自己的策略集

节点间只有一种关系：父子。整体构成一棵树：

```javascript
元课程（根）——研究对象：学习者本人
├── 课程：行测
│   ├── 会话……
│   └── （可预留子课程：资料分析）
├── 课程：申论
└── 课程：篮球投篮力学
```

* **元课程自洽性**：根节点的"学科"就是学习者本人。其建模插件建模"这个人整体"；其"大纲"是跨课程调度、阶段反思、心理疏导；其 agent 即导师。导师不凌驾于架构之上，只是恰好在教一门以学生为教材的课。
* **深度纪律：先冻结三层**（元课程 / 课程 / 会话）。子课程能力 schema 允许、发行版默认不用——架构留椅子，但不急着让人坐。
* **事件不动纪律**：课程拆分/合并只改树的元数据，事件永远留在被写入的节点。断言可重新投影，事件不重写。

### 2.2 事件

**信封（所有事件共有）**：

|字段|说明|
|-|-|
|id|全局唯一|
|timestamp|事件发生时间|
|schema\_version|事件 schema 版本号|
|actor|user / agent（含具体节点 agent）/ system / 具体插件|
|type|事件族 + 动词（xAPI 式 Actor-Verb-Object 语义核心）|
|provenance|产生它的插件 id+版本、策略 id+版本|
|session\_ref|所属会话|
|scope|节点路径（课程树作用域）|
|content\_ref|内容指针（大 payload 不内嵌，进内容存储）|

**演进纪律**：

* 只做加法：加字段可以，改语义、删字段不行；重放必须对历史所有事件永远有效。
* 事件寿命比写出它的代码长：插件会换十轮，事件还在。事件 schema 是冻结优先级最高的接口。
* 可选轴字段（深度、自发性等）后续版本补，旧事件无此字段仍合法。

**写入判断口诀**："这件事单独拿出来，对学习诊断有没有意义？"有则独立成事件，无则降级为属性。

### 2.3 投影

* 投影是插件；原始的还是原始的，派生的永远可以重新派生。
* 抽象梯子（投影的层级视图，非内核结构）：
* L0 操作层（点击、输入过程——由记录器插件选择性落盘）
* L1 交互层（xAPI 式原子学习交互，逐字、逐次、带内容指针）
* L2 证据层（四元组×轴的分类事件，带指向 L1 的出处指针）
* L3 断言层（学习者模型，证据汇聚自 L2）
* L4 决策层（策略的预测与回顾，消费 L3）
* 投影产出的双消费模式：学习者建模要**流式全量汇聚**；上下文管理要**按预算点查**。存储小要求：L2 事件须按 topic 可索引。
* 投影必须支持**否定式查询**（"自事件 X 后 7 天内无同类错误事件"——什么都没发生也是数据）。

### 2.4 Handoff（交接）

* 一等事件 `HandoffInitiated`：源节点 agent → 目标子节点 agent，含交接缘由与**交接上下文包**（相关断言、建议起点）。
* 接收方将交接包作为入口语境喂给自己的 context compiler，随后独立编译自己的上下文。**导师递条子，不代课。**
* 交接是任意父→子的统一操作，全部可审计：三个月后能查"这次路径转向基于什么判断发起"。

### 2.5 Agent 注册表

* 区分**持久 agent**（有身份、有自己的事件流、有持续关系：教学 agent、资料整理 agent、插件开发 agent……）与**一次性 function call**（无身份、无状态）。
* 内核冻结注册表语法（能力描述格式、作用域权限声明）；注册什么 agent 是发行版与插件的事。
* 全局 agent 的"导览"本质 = 在注册表上做服务发现。
* **权限模型**：默认任何插件只能读自己作用域的事件；上行投影是唯一的跨作用域通道。

\---

## 3\. 角色与事件词汇表

事件系统按参与角色组织（actor 字段的取值空间），各角色词汇生态独立演化：

### 3.1 User 侧：四元组

**answer / ask / interpret / explain** ——全部是生成性行为（generative acts），测量的是生产而非暴露。

* 理论地位：self-explanation effect、generation effect、learning by teaching、ICAP 框架（Interactive > Constructive > Active > Passive）均支持生成性行为作为学习证据；进度条/清单测量暴露，落于最低级。
* 证据强度阶梯：explain > interpret > ask > answer。"学会一个知识点"操作化为**该知识点的行为证据沿阶梯上移**。
* 反奖惩立场：奖惩挂进度是奖励浪费时间；挂四元组则触发 Goodhart 定律且可被 AI 代写作弊。生成行为停滞时**把数据显示给用户**（显化），而非扣分。

**分类的三个轴**（分类器插件的输出契约）：

```javascript
ActClassified {
  ref:          原始对话事件指针,
  act:          answer | ask | interpret | explain | …(可扩展动词表),
  topics:       \\\[知识点引用],
  depth:        recall | restructure | transfer,   // 可选，后续版本补
  spontaneity:  prompted | spontaneous,            // 可选，后续版本补
  confidence,
  classifier:   分类器 id + 版本
}
```

* 动作类型为必填；depth/spontaneity 为可选轴，additive 演进。
* spontaneity 证据权重极大：被提示行为与自发行为不是同一种证据（自发 explain 几乎不可能伪造）。
* 四元组×深度轴是检测"理解假象"的仪器：消费行为激增而生成行为停滞 → harness 显式警示。
* 分类器插件化：订阅对话事件、产出分类事件的投影插件。支持多级（便宜模型全量过、低置信度交强模型复核、复核结果作为新事件修正）。分类会错→分类是投影不是存储→换分类器重放即可翻新。
* 动词表开放注册（apply/create/运动领域的 do 等由未来插件扩展）；内核冻结信封与机制，动词表保持开放。

### 3.2 Agent 侧：tracing 传统

* 借鉴成熟实践（LLM 调用链 tracing / OTel GenAI 语义约定）：function call、回复、LLM 响应记为 span 式事件。
* LH 特有的承重作用：**策略归因**——策略回顾要求日志准确记录"动作到底是什么"（策略版本、模型、提示词、token 消耗）。
* 成本透明（开源工具，用户看得到 token 花在哪）由此侧事件天然提供。

### 3.3 System 侧

* 生命周期、错误、资源变更，按系统日志传统处理，无新东西。

\---

## 4\. 学习者建模（L3 断言层）

### 4.1 断言模型

* 不做确定性画像（"掌握度 72%"），做**带出处和置信度的断言集**：

> "系统认为用户在极限计算上薄弱；依据：事件 #341、#389；置信度：中。"

* 两个性质：**可审计**（每条判断回溯到证据）；**可争议**（用户可看到"系统觉得我弱在哪"并反驳——反驳本身是极高质量的学习事件）。

### 4.2 分域建模与上行

* 不同课程可用不同建模插件（篮球课与微积分课建模逻辑天然不同）：同一断言格式，不同生成逻辑，互不污染。
* \*\*信息沿树向上流动，且只以 L3 断言形式流动；原始事件永不跨作用域。\*\*元课程建模插件订阅子课程的断言投影，不是原始事件。
* 宏观断言引用子课程断言作证据（出处链跨节点完整）。
* **冲突是被表示的，不是被消灭的**：大部分"冲突"是表观冲突（不同维度看似矛盾）。全局模型不覆盖、不裁决课程断言，只持有自己的宏观断言层。用户看到矛盾并反驳的通道永远敞开。

### 4.3 待决：知识点体系

四元组证据的落脚点（topics 指向什么）尚未定案：从用户资料自动抽取 / 用户自建 / 混合。这决定分类器把证据挂到哪。

\---

## 5\. 策略层（L4）

### 5.1 策略作为数据，不是代码

* 策略 = 声明式片段：自然语言描述 + 适用条件 + 约束 + 预期产出，由模型在运行时解释执行。
* 插件贡献策略片段，内核提供解释器与评估回路。
* 懂教学不懂代码的人可编辑策略；策略可被系统生成与修改，在真实事件中被检验——坏的策略被数据淘汰，这是"可成长、有生命力"的技术形态。

### 5.2 教学动作携带预测（强制字段）

每个教学动作事件必须携带：

* **目标断言**：预期在学习者模型上引起什么变化；
* **预期证据**：以什么事件为证（行为化表述，如"两周内对 X 产出无提示 explain"、"prompted 行为转化为 spontaneous"）；
* **时间窗**。

### 5.3 策略评估 = 假设检验，与学习者建模耦合

* 动作是干预，学习者模型是测量仪器，后续事件流是观测数据。策略回顾投影定期对账：兑现的策略加分，落空的降权。
* **测量仪器质量是策略评估的天花板**：先投资学习者模型的测量质量（证据出处、置信度校准），再投资策略评估的精巧度。MVP 阶段策略回顾可以是纯 LLM 判断，但预测字段必须从现在记——后补无数据。
* **探索动作**：一类存在意义纯粹是测量的教学动作（诊断性测验、探边界的问题），服务于学习者模型覆盖率，防止"只看得见策略让看见的东西"的盲区自我加固。这是策略解释器的内置义务。
* **失败模式诊断**：讲解策略后 explain 增多但全是复述性深度 → 策略在制造理解假象，应被降权。

### 5.4 资源模型（因材施教的另一面）

* 资料是一等公民：覆盖知识点、难度、形式、可信度等元数据。
* 施教 = 学习者当前状态 × 手头资源可用性的匹配；无资源模型则"因材施教"退化为凭空生成。
* 删除不变量：用户删除某资料后，所有派生状态重建不含该资料任何痕迹。

\---

## 6\. 上下文管理

### 6.1 三个子问题

1. **存储**：事件日志天然解决（追加、不可变、可重放）。
2. **重建**：context compiler——输入当前情境 + 事件历史 + 学习者模型，输出 token 预算约束下的上下文包。是编译问题不是检索问题：还要回答"以什么粒度装、装不下的以摘要还是指针形式存在"。
3. **预算分配**：分工的价值不在"多 agent 对话"，而在"每个角色只需很窄的上下文视图"。导师只看 L3 断言，课程 agent 只编译自己作用域——窄视野 = 便宜 + 可测 + 可替换。

### 6.2 抽象即压缩

* 通用 LLM 摘要的损失不可控、不可回溯、不可验证。抽象梯子提供的压缩：**损失结构化**（知道丢了什么、留了什么）、**可回溯**（指针链一路指回原文）、**可重算**（换分类器重放即翻新）。
* context compiler 的工作方式：**按预算沿梯子下潜**——预算紧装 L3 断言；预算松展开 L2 行为摘要；需要逐字证据时沿指针取 L1 原文。

### 6.3 多 agent 纪律

* 不为多 agent 而多 agent。早期一个 orchestrator + 若干插件；某部件上下文需求撑爆预算时再拆——事件溯源保证拆分可事后做、不用重写。
* 导师职责负面清单（防 god object）：**不教学、不批改、不访问课程原始事件、不修改课程断言**。只做四件事：宏观断言、跨课程调度、发起元会话、路由交接。

\---

## 7\. 架构验证方法

一人 + coding agent 工作流的最大风险是**架构漂移**。对策：把架构从一张图变成一组会自动报警的断言。

### 7.1 不变量清单（每条配可执行检查）

1. 给定同一插件集合，从事件日志重放重建状态，两次重放结果一致。
2. 任何一次上下文组装输出不超过 token 预算；超预算走降级策略，不得截断。
3. 每个教学动作事件携带预测字段，且引用学习者模型中的至少一条证据。
4. 插件只能向事件日志追加，不得直写任何投影/物化视图。
5. 资料删除后，派生状态重建不含该资料痕迹。
6. 原始事件不跨作用域；跨节点的信息流只以断言形式上行。
7. 全部历史事件在新版本 schema 下仍可重放（additive 演进的回归检查）。
8. 事件永不迁移节点（课程重组只改元数据）。

### 7.2 黄金场景测试（端到端）

1. **中断回归**：用户学完第三章，中断 18 天，回来发"我们继续吧"——检验持久化、上下文重建、遗忘处理、模型调取、计划续接。
2. **跨课程调度**：导师基于多课程断言发起 handoff，课程 agent 正确接收入口语境。
3. **资料生命周期**：导入 → 索引 → 因材匹配 → 删除 → 重放验证无痕迹。
4. **分类器升级**：换新版分类器重放历史，断言层正确翻新且无残留。
5. **用户争议**：用户反驳某断言，争议事件入日志，断言置信度正确演化。

### 7.3 配套工具

* **ADR（架构决策记录）**：agent 修改内核前必须读；防跨会话漂移。
* **模拟学习者**：假用户把数周事件压缩进几分钟灌入——学习产品的 bug 大多和时间有关，而时间不花真钱就能加速。
* **fitness functions 思路**：陈述不变量是人的智力工作，写检查脚本是 coding agent 的体力工作。

\---

## 8\. 宿主平台路线（dsh / Cordis）

### 8.1 评估结论

dsh 与 LH 架构同源（session log 即 append-only 事件源、模型历史为投影、`agent/pre-step` 上下文拦截点、一切皆插件、MIT），是 LH 内核观的真实参考实现——**当镜子和验证场，不当地基**。主要风险：开发者预览版明确将有破坏性变更（已有移除 SQLite 后端先例），底层 Cordis API 未完全稳定；session 为任务级、UI 为开发者向，与 LH 的用户群（非 AI 非编程专业学习者）错配。

### 8.2 三段式路线

1. **寄生验证（现在）**：在 dsh 上实现 LH 核心插件 MVP——对话记录器、LLM 四元组分类器、学习者模型投影、挂在 `agent/pre-step` 的 context compiler。目标是检验架构，不是产品。
2. **契约自持（第一天起）**：LH 事件词汇表（信封、四元组、断言格式）定义在自己的 schema 文件里，插件只做映射。dsh 是可替换宿主，上游炸了带着契约和数据迁走。
3. **分岔点（看产品形态）**：定型为自托管 power-user 工具 → 留在 dsh 生态甚至发行 profile；走向面向普通学习者的产品 → 带已验证的架构与契约自建壳。

### 8.3 Cordis 单独采用

* 依赖管理痛点可只依赖 Cordis 库解决（服务定位 + 声明式注入 + 生命周期 + 可逆副作用），不必引入整个 dsh。
* 换来：可替换性（LLM 服务换实现不改消费插件）、加载顺序自动解析、**测试替身免费**（golden scenario 中换假 LLM 服务、内存事件存储 → 系统确定性化）。
* 注意：DI 解决依赖解析，不解决契约演化；服务契约属内核级语法，版本化、只加不改。

\---

## 8.4 "重复造轮子"审查（2026-09 快照）

对 LH 全部部件的现有轮子盘点。原则：**产品价值应集中在少数真正新的东西上，其余全是成熟轮子的可靠组装。**

### A. 直接采用（不造）

|部件|现成轮子|采用方式|
|-|-|-|
|学习事件动词表|xAPI 动词注册表（社区驱动演化十余年，含扩展机制）|事件信封保持自有（xAPI 无作用域/出处/投影概念），动词表沿用 xAPI；四元组映射为 answered/asked/…；免费获得 Moodle/H5P 生态互操作|
|间隔重复调度|FSRS（MIT，py/ts/Rust 多语言实现，Anki 默认调度器，5 亿+ 复习记录验证）|集成 ts-fsrs 为调度插件：策略层决策"何时复习什么"，FSRS 计算具体排期——判断/机制分工|
|掌握度建模|知识追踪（BKT/DKT 三十年谱系 + 开源实现；LLMKT 验证了 LLM 对话式知识追踪路线）|BKT 实现作为官方建模插件之一；断言模型是表示层，KT 是证据生成逻辑的一种——验证建模插件可插拔|
|依赖注入容器|Cordis|见 8.3|

### B. 借鉴思想，不采用

|领域|代表|借鉴什么|为什么不采用|
|-|-|-|-|
|Agent 记忆层|mem0 / Letta / Zep|Letta 的分层思想（core/archival ↔ 我们的抽象梯子）作为 context compiler 预算分配参考；mem0 可作可选插件管"用户偏好/生活事实"类非学习语义记忆|它们解决"对话 agent 的事实记忆"，无出处链、无重放、无作用域——而这三者正是 LH 核心价值|
|LLM 导师系统|AutoTutor+LLM / OATutor / LPITutor|教学法 prompt 工程（苏格拉底式追问、护栏、提示分级、学习风格调节）是官方策略包的最佳素材库|全部是单课程单会话的 tutor，无人做跨课程、数月尺度、事件溯源的 harness——LH 的定位本身无轮子|
|企业级 LRS|Learning Locker / Trax|API 设计与查询模式（按 actor/verb/时间过滤）作为事件查询层参考|面向企业培训数据管道的重型服务，无投影机制、无插件体系、无事件溯源语义|

### C. 真正自造（LH 独特性收敛为四件）

1. **领域化事件溯源组装**：append-only 日志 + 投影 + 重放 + 出处链，以学习语义组织的投影梯子——内核，产品价值所在。
2. **四元组×三轴证据层**：从对话流产出可争议证据事件的分类器插件体系，无现成开源件。
3. **策略-预测-回顾闭环**：策略作为数据 + 教学动作携带预测 + 事件流假设检验，ITS 文献中仅有零星对应物，最接近"无人做过"。
4. **课程树 + 导师模型**：断言上行、handoff 下行的三层结构。

### D. 审查对文档的修订

* 待决清单第 4 项收窄：知识点**元数据格式**参考现有教育标准（IEEE LOM / 课程标准映射），不发明格式；知识点图谱内容仍需抽取或自建，是 LH 必须自己长出来的部分。
* 发行版插件清单可写实：`xapi-vocab`（词汇）、`fsrs-adapter`（调度）、`bkt-modeler`（掌握度）、`classifier-quadruple`（四元组）、`learner-model-core`（断言）、`context-compiler`（pre-step 挂载）——约半数有现成实现垫底。

\---

## 9\. 技术实现待决清单（下一阶段）

1. **服务契约清单 + agent 注册表语法**：`event-store` / `llm` / `content-store` / `learner-model` / `classifier` 各自的接口形状、依赖方向、版本策略。
2. **事件存储选型**：追加日志的物理实现（文件 / SQLite / 专用库）、索引方案（按 topic / scope / 时间的点查 + 否定式查询）。
3. **内容存储与指针方案**：大 payload 的存取、引用完整性、删除传播。
4. **知识点体系**：topics 的来源（资料自动抽取 / 用户自建 / 混合）——决定四元组证据的落脚点。元数据格式参考 IEEE LOM / 课程标准映射，不发明格式；图谱内容需抽取或自建。
5. **LLM 服务抽象**：多模型适配、成本计量（dsh 式 token 透明）、便宜模型起步的默认配置。
6. **前端形态**：学习者的主界面（刷题、知识点视图、四元组证据可视化、断言的可查看与可争议入口）与开发调试界面的分离。
7. **发行版插件清单**：默认捆绑的记录器组、官方分类器、官方策略包、官方建模插件。基础版见 8.4-D：`xapi-vocab` / `fsrs-adapter` / `bkt-modeler` / `classifier-quadruple` / `learner-model-core` / `context-compiler`。
8. **词汇表草案**：三个角色各自的事件族 + 动词表第一版；user 侧动词直接映射 xAPI 注册表（见 8.4-A）。

\---

## 附：核心概念速查

|概念|归属|一句话|
|-|-|-|
|节点（课程树）|内核|作用域 + 日志 + agent + 建模插件 + 策略集|
|事件|内核（信封）/ 插件（词汇）|append-only，带出处与作用域，additive 演进|
|投影|机制内核 / 逻辑插件|原始的可重放，派生的可重算|
|四元组|插件（官方约定）|answer/ask/interpret/explain，生成性行为证据|
|断言|插件|带出处与置信度，可审计可争议，只沿树上行|
|策略|数据（插件贡献）|声明式片段 + 强制预测字段，被证据流筛选|
|context compiler|内核机制|按预算沿抽象梯子下潜的编译器|
|handoff|内核机制|父→子的带语境交接事件，可审计|
|导师|元课程 agent|宏观断言、调度、元会话、路由；其余禁止|
|注册表|内核语法|持久 agent 有身份，function call 无身份|



